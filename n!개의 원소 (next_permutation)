int T;
scanf("%d", &T);
arr.resize(T);
for (int i = 0; i < T; i++)
	scanf("%d", &arr[i]);
sort(arr.begin(), arr.end());
int ans = -1;
do
{
	for (int i = 0; i < T; i++)
		printf("%d ", arr[i]);
	printf("\n");
} while (next_permutation(arr.begin(), arr.end()));
